#include<stdio.h>
// Write a program to print first n natural numbers using for loop ..
int main()
{
    for(int a=0; a<10; a++){ //(Initialize; Test; Increment or decrement)
        printf("The naturals numbers are %d \n", a+1);
    }
    return 0;

    // Initialize -> setting a lo0p counter to an initial value.
    // Test -> checking a condition.
    // Increment -> updating the loop counter..
}